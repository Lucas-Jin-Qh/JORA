{
  "description": "OFT configuration files generated by script. Each file configures OFT (Orthogonal Fine-Tuning) for a specific base model, dataset and rank.",
  "note": "Either 'r' (non-zero) or 'oft_block_size' (non-zero) must be set, not both. These configs set 'r' and keep 'oft_block_size' as 0."
}

